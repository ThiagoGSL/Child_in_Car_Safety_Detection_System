/// Define os estados possíveis para o processo de check-in.
enum CheckinStatus { idle, pending, confirmed, timeout }

/// Define os estados possíveis do veículo.
enum VehicleState { moving, stopped, unknown }